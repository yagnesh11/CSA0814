def second_max(num):
    if len(num)<2:
        return"Not enough elements"
    sorted_num=sorted(num)
    return sorted_num[-2]
num_list=[1,5,9,8,2]
result=second_max(num_list)
print("second_max:", result)
