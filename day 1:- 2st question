a=int(input("Enter the first number:"))
b=int(input("Enter the second number: "))
def gcd(a,b):
    while b:
        a,b=b,a%b
    return a
def lcm(a,b):
    return (a*b)/gcd(a,b)
print("The 1cm of", a, "and", b, "is", lcm(a,b))
